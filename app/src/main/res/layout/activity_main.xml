<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:paddingLeft="24dp"
    android:paddingRight="24dp"
    tools:context="com.example.it2.axpresslogisticapp.acitvities.MainActivity">

    <ImageView
        android:id="@+id/splashicon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="200dp"
        android:src="@mipmap/ic_launcher24s" />

    <TextView
        android:id="@+id/logo_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/splashicon"
        android:layout_centerHorizontal="true"
        android:text="@string/app_name"
        android:textColor="@color/text_iconColor"
        android:textSize="18sp"
        android:textStyle="italic"
        android:layout_marginBottom="40dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo_text"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:layout_marginTop="20dp"
        android:orientation="vertical">

        <include
            android:id="@+id/btn_login"
            layout="@layout/primary_button_large" />

        <include
            android:id="@+id/btn_signup"
            layout="@layout/primary_button_large" />

    </LinearLayout>


</RelativeLayout>
